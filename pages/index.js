export default function HomePage() {
  return (
    <div>
      <h1>SHA-256 API</h1>
      <p>Send a POST request to /api/sha256 with a "text" parameter to get the SHA-256 hash.</p>
    </div>
  );
}
